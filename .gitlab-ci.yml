# This file is generated by GitLab CI
Clang (Release):
  script:
  - git submodule update --init
  - ls -la
  - docker run --rm -e "CC=clang" -e "CXX=clang++" -v `pwd`:/home/gitlab/Qubus -w
    /home/gitlab/Qubus td-quantum/archlinux-ci ./run_tests.sh
  tags:
  - docker
  - cuda
GCC (Release):
  script:
  - git submodule update --init
  - ls -la
  - docker run --rm -e "CC=gcc" -e "CXX=g++" -v `pwd`:/home/gitlab/Qubus -w /home/gitlab/Qubus
    td-quantum/archlinux-ci ./run_tests.sh
  tags:
  - docker
  - cuda
Build documentation:
  script:
  - ls -la
  - docker run --rm -v `pwd`:/home/gitlab/Qubus -w /home/gitlab/Qubus td-quantum/archlinux-ci
    ./build_doc.sh
  - cp -r build/qubus/doc/* /home/gitlab/doc/latest
  - scp -r build/qubus/doc/html/* supas335@upload.rz.uni-kiel.de:htdocs/qubus/doc
  type: deploy
  tags:
  - docker
  only:
  - master
